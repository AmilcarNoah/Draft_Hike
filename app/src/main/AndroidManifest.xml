<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.amilcarf.draft_hike">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Optional: For emergency call feature; NOT SURE if we include it -->
    <!--
    <uses-permission android:name="android.permission.CALL_PHONE" />
    -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BenchFinder">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BenchFinder.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Trails List Activity -->
        <activity
            android:name=".TrailsListActivity"
            android:label="@string/trails_with_benches"
            android:parentActivityName=".MainActivity" />

        <!-- Trail Detail Activity -->
        <activity
            android:name=".TrailDetailActivity"
            android:label="Trail Details"
            android:parentActivityName=".TrailsListActivity" />

        <!-- Navigation Activity -->
        <activity
            android:name=".NavigationActivity"
            android:label="Trail Navigation"
            android:parentActivityName=".TrailsListActivity" />

        <!-- Map Activity -->
        <activity
            android:name=".MapActivity"
            android:label="Find Benches"
            android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.BenchFinder.FullScreen" />


        <!-- Google API Info; Research on Security for this!!! -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />


        <!-- Settings  -->
        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings"
            android:parentActivityName=".MainActivity" />



    </application>

</manifest>